projection implementation in class ZBP_C_2305FLIGHT unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_2305FLIGHT alias FLIGHT
extensible
use etag

{
//  use create;
  use update;
//  use delete;
field (readonly) PlaneTypeID;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}